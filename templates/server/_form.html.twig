{{ form_start(form) }}

    <div class="form-group {{ form.name.vars.valid==false ? "has-error" }}">
        {{ form_label(form.name) }}
        {{ form_widget(form.name) }}
        {{ form_errors(form.name) }}
    </div>

    <div class="form-group {{ form.host.vars.valid==false ? "has-error" }}">
        {{ form_label(form.host) }}
        {{ form_widget(form.host) }}
        {{ form_errors(form.host) }}
    </div>

    <div class="form-group {{ form.port.vars.valid==false ? "has-error" }}">
        {{ form_label(form.port) }}
        {{ form_widget(form.port) }}
        {{ form_errors(form.port) }}
    </div>

    <div class="form-group {{ form.username.vars.valid==false ? "has-error" }}">
        {{ form_label(form.username) }}
        {{ form_widget(form.username) }}
        {{ form_errors(form.username) }}
    </div>

    <div class="form-group {{ form.password.vars.valid==false ? "has-error" }}">
        {{ form_label(form.password) }}
        {{ form_widget(form.password) }}
        {{ form_errors(form.password) }}
    </div>

    <div class="form-group {{ form.rsa_key.vars.valid==false ? "has-error" }}">
        {{ form_label(form.rsa_key) }}
        {{ form_widget(form.rsa_key) }}
        {{ form_errors(form.rsa_key) }}
    </div>

    <button type="submit" class="btn btn-dark">{{ button_label|default('Sauvegarder') }}</button>

    {{ form_widget(form) }}
    
{{ form_end(form) }}